class Movie {
  constructor (id,category,title, year,imdbID, type, poster) {
    this.id = id;
    this.category = category;
    this.title = title
    this.year = year
    this.imdbID = imdbID
    this.type = type
    this.poster = poster
  }
}

export default Movie